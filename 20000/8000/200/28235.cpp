#include <iostream>
#include <string>

int main()
{
    std::string str;
    std::cin >> str;
    if(str == "SONGDO")
        std::cout << "HIGHSCHOOL";
    if(str == "CODE")
        std::cout << "MASTER";
    if(str == "2023")
        std::cout << "0611";
    if(str == "ALGORITHM")
        std::cout << "CONTEST";
}